# Deployment records

This folder answers where a public app lives without storing a credential.
Create one Markdown record per deployed website or app from `_template.md`.

The record is an inventory and rollback aid. It is not proof by itself: the
actual provider, exact deployed revision, public URL, and live journey still
need to be checked when the result matters.
